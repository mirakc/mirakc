name: ci

on:
  push:
    branches:
      - main
      - release
      - trial-*
    paths-ignore:
      - '**.md'

jobs:
  build:
    uses: mirakc/mirakc/.github/workflows/build.yml@main
    secrets: inherit
  cross-build:
    uses: mirakc/mirakc/.github/workflows/cross-build.yml@main
    secrets: inherit
